        /* ============================================================
           07. ESTILOS DAS DOBRAS (2 A 11)
           ============================================================ */
        .section-padding {
            padding: 6rem 2rem;
            position: relative;
        }

        .section-dark {
            background-color: var(--color-graphite);
            color: var(--color-surface);
        }

        .section-header {
            text-align: center;
            max-width: 680px;
            margin: 0 auto 4rem auto;
            display: flex;
            flex-direction: column;
            gap: 1.2rem;
        }

        .section-title {
            font-family: var(--font-display);
            font-size: clamp(2.5rem, 5vw, 4.5rem);
            line-height: 1;
            letter-spacing: 0.02em;
            color: var(--color-text);
        }

        .section-dark .section-title {
            color: var(--color-surface);
        }

        .section-desc {
            font-size: 1.125rem;
            line-height: 1.6;
            color: var(--color-muted);
        }

        .section-dark .section-desc {
            color: rgba(255, 255, 255, 0.7);
        }

        /* Mix / Categorias */
        .mix-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            gap: 2rem;
            flex-wrap: wrap;
            border-top: 1px solid var(--color-beige);
            padding-top: 3rem;
        }

        .mix-item {
            flex: 1;
            min-width: 220px;
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1.5rem;
            background: var(--color-surface);
            border: 1px solid rgba(229, 216, 197, 0.5);
            border-radius: 12px;
            box-shadow: var(--shadow-subtle);
            transition: transform var(--transition-fast), box-shadow var(--transition-fast);
        }

        .mix-item:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-medium);
        }

        .mix-icon {
            font-size: 2rem;
            color: var(--color-accent);
            background: rgba(242, 194, 26, 0.1);
            width: 56px;
            height: 56px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            flex-shrink: 0;
        }

        .mix-text {
            font-weight: 600;
            font-size: 0.95rem;
            color: var(--color-graphite);
        }

        /* Grade de Produtos (Cortes, Complementos) */
        .product-grid {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2.5rem;
        }

        .product-card {
            background: var(--color-surface);
            border-radius: 16px;
            overflow: hidden;
            border: 1px solid rgba(229, 216, 197, 0.5);
            box-shadow: var(--shadow-subtle);
            display: flex;
            flex-direction: column;
            transition: transform var(--transition-normal), box-shadow var(--transition-normal);
        }

        .product-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-medium);
        }

        .product-image-wrap {
            position: relative;
            width: 100%;
            padding-top: 100%; /* 1:1 Aspect Ratio */
            overflow: hidden;
            background: var(--color-surface-subtle);
        }

        .product-image-wrap img {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 1.2s var(--ease-out-expo);
        }

        .product-card:hover .product-image-wrap img {
            transform: scale(1.05);
        }

        .product-content {
            padding: 2rem;
            display: flex;
            flex-direction: column;
            flex: 1;
        }

        .product-name {
            font-family: var(--font-display);
            font-size: 1.8rem;
            letter-spacing: 0.03em;
            margin-bottom: 0.5rem;
            color: var(--color-text);
        }

        .product-desc {
            font-size: 0.9rem;
            line-height: 1.5;
            color: var(--color-muted);
            margin-bottom: 1.5rem;
            flex: 1;
        }

        .product-action {
            align-self: flex-start;
            font-weight: 600;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: var(--color-wood);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            border-bottom: 1px solid transparent;
            padding-bottom: 2px;
            transition: border-color var(--transition-fast), color var(--transition-fast);
        }

        .product-action:hover {
            color: var(--color-text);
            border-color: var(--color-text);
        }
        
        .disclaimer-text {
            text-align: center;
            font-size: 0.8rem;
            color: var(--color-muted);
            margin-top: 3rem;
            font-style: italic;
        }

        /* Editorial Split (Destaque e Loja) */
        .editorial-split {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 5rem;
            align-items: center;
        }

        .editorial-content {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            gap: 1.5rem;
        }

        .editorial-image {
            position: relative;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: var(--shadow-medium);
        }
        
        .editorial-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            aspect-ratio: 4/5;
        }

        /* FAQ Accordion */
        .faq-container {
            max-width: 800px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .faq-details {
            background: var(--color-surface);
            border: 1px solid rgba(229, 216, 197, 0.5);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: var(--shadow-subtle);
        }

        .faq-summary {
            padding: 1.5rem 2rem;
            font-weight: 600;
            font-size: 1.05rem;
            color: var(--color-text);
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            list-style: none;
            transition: background-color var(--transition-fast);
        }

        .faq-summary::-webkit-details-marker {
            display: none;
        }

        .faq-summary:hover {
            background-color: var(--color-surface-subtle);
        }

        .faq-icon {
            font-size: 1.2rem;
            color: var(--color-wood);
            transition: transform var(--transition-fast);
        }

        .faq-details[open] .faq-icon {
            transform: rotate(180deg);
        }

        .faq-content {
            padding: 0 2rem 1.5rem 2rem;
            font-size: 0.95rem;
            line-height: 1.6;
            color: var(--color-muted);
        }

        /* Localização Grid */
        .location-grid {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }
        
        .info-list {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
            margin-bottom: 2.5rem;
        }

        .info-item {
            display: flex;
            gap: 1rem;
            align-items: flex-start;
        }

        .info-item-icon {
            font-size: 1.5rem;
            color: var(--color-accent);
            margin-top: 2px;
        }

        .info-item-title {
            font-weight: 600;
            font-size: 1rem;
            color: var(--color-graphite);
            margin-bottom: 0.25rem;
        }

        .info-item-text {
            font-size: 0.9rem;
            line-height: 1.5;
            color: var(--color-muted);
        }

        /* Rodapé */
        .site-footer {
            background: var(--color-graphite);
            color: var(--color-surface);
            padding: 5rem 2rem 2rem 2rem;
        }

        .footer-grid {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            gap: 4rem;
            padding-bottom: 4rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .footer-brand .brand-title {
            color: var(--color-surface);
        }
        
        .footer-brand .brand-subtitle {
            color: var(--color-accent);
        }

        .footer-desc {
            margin-top: 1.5rem;
            font-size: 0.95rem;
            line-height: 1.6;
            color: rgba(255, 255, 255, 0.6);
            max-width: 320px;
        }

        .footer-nav-title {
            font-weight: 700;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--color-surface);
            margin-bottom: 1.5rem;
        }

        .footer-nav-list {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .footer-nav-link {
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.95rem;
            transition: color var(--transition-fast);
        }

        .footer-nav-link:hover {
            color: var(--color-accent);
        }

        .footer-bottom {
            max-width: 1400px;
            margin: 2rem auto 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.85rem;
            color: rgba(255, 255, 255, 0.4);
        }

        /* Responsividade Dobras */
        @media (max-width: 992px) {
            .editorial-split, .location-grid, .footer-grid {
                grid-template-columns: 1fr;
                gap: 3rem;
            }
            .mix-item {
                min-width: calc(50% - 1rem);
            }
        }
        @media (max-width: 640px) {
            .section-padding { padding: 4.5rem 1.25rem; }
            .mix-item { min-width: 100%; }
            .footer-grid { grid-template-columns: 1fr; gap: 2.5rem; }
            .footer-bottom { flex-direction: column; gap: 1rem; text-align: center; }
            .editorial-image img { aspect-ratio: 1/1; }
        }
